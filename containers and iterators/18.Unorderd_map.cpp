//everything same as map, but it does not stores it any order
//0(1) in almost all cases, but 0(n) in worst case
//it cannot store pairs(imp)
#include <bits/stdc++.h>
using namespace std;

int main() {
    
    unordered_map<string,int> mp;

    return 0;
}